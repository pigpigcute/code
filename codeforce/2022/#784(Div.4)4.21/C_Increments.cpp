#include <bits/stdc++.h>
using namespace std;

int main()
{
    int i, t, n, a[60];
    cin >> t;
    while (t--)
    {
        cin >> n;
        for (i = 1; i <= n; i++)
            cin >> a[i];
    }
    

    return 0;
}